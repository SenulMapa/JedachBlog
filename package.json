{
  "name": "dev-notes-blog",
  "version": "1.0.0",
  "description": "Markdown-based blog builder",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "new": "node build.js --new"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "gray-matter": "^4.0.3"
  }
}